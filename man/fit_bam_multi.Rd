% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bam_multi.r
\name{fit_bam_multi}
\alias{fit_bam_multi}
\title{fit_bam_multi
Fit a Generalized Additive Model using bam() with factor-smooth interactions to butterfly count data across multiple years simultaneously.
This function fits a single model across multiple years using factor-smooth interactions (fs), which allows year-specific smooth terms
while sharing information across years. This is more efficient than fitting separate models per year.}
\usage{
fit_bam_multi(
  dataset_multi,
  NbrSample = NULL,
  GamFamily = "poisson",
  MaxTrial = 4,
  TimeUnit = "d",
  MultiVisit = "mean",
  weekday = 3,
  tp_col = NULL,
  smooth_basis = "cr",
  k_value = -1,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{dataset_multi}{data.table Butterfly counts for species x over multiple years y over all sites.}

\item{NbrSample}{integer Maximum number of sites to sample per year, default=NULL (use all sites).}

\item{GamFamily}{string Family for GAM, default='poisson', but can be 'nb' or 'quasipoisson'.}

\item{MaxTrial}{integer Maximum number of fitting attempts, default=4.}

\item{TimeUnit}{character Time-step for the spline, 'd' for day or 'w' for week.}

\item{MultiVisit}{string Function for summarising multiple counts within a time unit, 'max' or 'mean' (default).}

\item{weekday}{Integer for selected day of the week for weekly summary, default is 3 (Wednesday). [1-7] where 1 = Monday.}

\item{tp_col}{string Name of temporal variable used in the GAM model, default NULL.}

\item{smooth_basis}{string Basis for the smooth term, default "cr" (cubic regression spline). Other options include "tp" (thin plate), "ps" (P-splines).}

\item{k_value}{integer Basis dimension for the smooth, default=-1 (automatic selection).}

\item{verbose}{logical Indicating if progress reports should be given.}

\item{...}{Additional parameters passed to bam function from the \link[mgcv]{bam} package.}
}
\value{
A list with three objects:
  i) **f_curve**: a data.table with the flight curves for all years with expected relative abundance (NM), normalized to sum to one per year,
  ii) **f_model**: the resulting bam model fitted on the count data across all years,
  iii) **f_data**: a data.table with the data used to fit the BAM model.
}
\description{
fit_bam_multi
Fit a Generalized Additive Model using bam() with factor-smooth interactions to butterfly count data across multiple years simultaneously.
This function fits a single model across multiple years using factor-smooth interactions (fs), which allows year-specific smooth terms
while sharing information across years. This is more efficient than fitting separate models per year.
}
\details{
This function uses factor-smooth interactions (s(tp_col, M_YEAR, bs="fs")) which allows:
  - Fitting a single model for multiple years (more efficient)
  - Year-specific smooths that can differ in shape
  - Sharing of information across years for more stable estimates
  - Better handling of years with sparse data
}
\seealso{
\link{flight_curve_multi}, \link{fit_gam}, \link[mgcv]{bam}
}
\author{
Reto Schmucki - \email{retoshm@ceh.ac.uk}
}
\keyword{bam,}
\keyword{factor-smooth}
\keyword{gam,}
