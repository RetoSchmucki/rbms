% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_bam_multi.r
\name{flight_curve_multi}
\alias{flight_curve_multi}
\title{flight_curve_multi
Compute flight curves across multiple years simultaneously using a single BAM model with factor-smooth interactions.
This is more efficient than fitting separate models per year and can provide better estimates for years with sparse data.}
\usage{
flight_curve_multi(
  ts_season_count,
  NbrSample = 100,
  MinVisit = 3,
  MinOccur = 2,
  MinNbrSite = 1,
  MaxTrial = 3,
  GamFamily = "poisson",
  CompltSeason = TRUE,
  SelectYear = NULL,
  TimeUnit = "d",
  MultiVisit = "mean",
  weekday = 3,
  tp_col = NULL,
  smooth_basis = "cr",
  k_value = -1,
  KeepModel = TRUE,
  KeepModelData = TRUE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{ts_season_count}{data.table Time-series of counts and season information returned by \link{ts_monit_count_site}}

\item{NbrSample}{integer Maximum number of sites to use per year, default=100.}

\item{MinVisit}{integer Minimum number of visits required for a site to be included, default=3.}

\item{MinOccur}{integer Minimum number of positive records required per year in a site, default=2.}

\item{MinNbrSite}{integer Minimum number of sites required per year, default=1.}

\item{MaxTrial}{integer Maximum number of trials for model convergence, default=3.}

\item{GamFamily}{string Distribution for GAM, default='poisson', but can be 'nb' or 'quasipoisson'.}

\item{CompltSeason}{logical Restrict to years where complete season was sampled, default=TRUE.}

\item{SelectYear}{integer Vector of specific years to include, default=NULL (use all).}

\item{TimeUnit}{character Time-step for the spline, 'd' for day or 'w' for week.}

\item{MultiVisit}{string Function for summarising multiple counts within a time unit, 'max' or 'mean' (default).}

\item{weekday}{Integer for selected day of the week for weekly summary, default is 3 (Wednesday). [1-7] where 1 = Monday.}

\item{tp_col}{string Name of temporal variable, default NULL (auto-determined from TimeUnit).}

\item{smooth_basis}{string Basis for smooth term, default "cr". Options: "tp", "ps", "cr".}

\item{k_value}{integer Basis dimension, default=-1 (automatic).}

\item{KeepModel}{logical Keep model output, default=TRUE.}

\item{KeepModelData}{logical Keep data used for fitting, default=TRUE.}

\item{verbose}{logical Show progress reports, default=TRUE.}

\item{...}{Additional parameters passed to bam function.}
}
\value{
A list with three objects:
  i) **pheno**: flight curves with expected relative abundance (NM) across all years,
  ii) **model**: the bam model object fitted across all years,
  iii) **data**: the data used to fit the model (if KeepModelData=TRUE).
}
\description{
flight_curve_multi
Compute flight curves across multiple years simultaneously using a single BAM model with factor-smooth interactions.
This is more efficient than fitting separate models per year and can provide better estimates for years with sparse data.
}
\details{
This function uses mgcv::bam with factor-smooth interactions which is particularly useful for:
  - Large datasets with many years
  - Years with varying data quality/quantity
  - Sharing information across years while allowing year-specific patterns
}
\seealso{
\link{fit_bam_multi}, \link{flight_curve}, \link[mgcv]{bam}
}
\author{
Reto Schmucki - \email{retoshm@ceh.ac.uk}
}
\keyword{curve,}
\keyword{factor-smooth}
\keyword{flight}
\keyword{gam,}
